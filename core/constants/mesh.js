// Default resource ownership values used by 'Mesh' instances, when ownership options are not provided
export const MESH_DEFAULTS = Object.freeze({
    OWNS_GEOMETRY : true,
    OWNS_MATERIAL : true
});
