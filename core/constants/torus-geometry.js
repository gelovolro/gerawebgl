// Default options, used by 'TorusGeometry'
export const TORUS_DEFAULTS = Object.freeze({
    MAJOR_DIAMETER   : 1.5,
    TUBE_DIAMETER    : 0.5,
    RADIAL_SEGMENTS  : 16,
    TUBULAR_SEGMENTS : 32
});

// Minimum accepted counts and values, used by 'TorusGeometry'
export const TORUS_LIMITS = Object.freeze({ MIN_SEGMENT_COUNT: 3 });
